---
guideVersion: 1.0.0
estimatedMinutes: 1
steps:
  - id: install
    title: Install the MCP server (and browsers)
    estSeconds: 60
    autoCompletedByInstall: true
---

# Playwright setup

No configuration. On install, Playwright will fetch the Chromium browser it
uses for automation (~200 MB). Wayland tracks the install progress for you.

You can launch other browsers (Firefox, WebKit) by adding optional packages
from the Playwright settings panel.
