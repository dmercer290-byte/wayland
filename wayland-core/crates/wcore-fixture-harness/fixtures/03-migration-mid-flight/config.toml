# SANITIZED FIXTURE — generated 2026-05-24 from migration-mid-flight — secrets redacted per .blackboard/E2E-FIXTURE-SANITIZATION-2026-05-24.md
# State: yaml->toml migration ran once (v0.8.1 -> v0.8.2 upgrade path).
# The [default] block is the migrated form. The legacy yaml still exists.
# jobs.json still has Desktop-app 'schedule' field (migration not yet re-serialized).
# One session is v0 (no schema_version field — the forward-compat ladder must run).
# Tests: schema migration ladder (B-7), cron alias deserialization (B-3), config resolution (B-11).

[default]
provider = "anthropic"
model = "anthropic/claude-opus-4.6"
max_tokens = 8192

# Migrated from config.yaml on 2026-01-01 (timestamp is normalized).
# The migrated_from field is engine-internal — not part of the public Config struct
# but written by the migration tool to mark the migration as complete.
migrated_from = "config.yaml"
migration_version = "v0.8.1"

[providers.anthropic]
base_url = "https://openrouter.ai/api/v1"

[tools]
auto_approve = false

[session]
enabled = true

[storage.credentials]
backend = "plaintext"
